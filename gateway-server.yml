server:
  port: 8080
spring:
  cloud:
    gateway:
      routes:
        - id: catalogRoute
          uri: lb://CATALOG-SERVICE
          predicates:
            - Path=/catalog/**
#           filters:
#             - name: CatalogFilter
        - id: movieRoute
          uri: lb://MOVIE-SERVICE
          predicates:
            - Path=/movies/**
#           filters:
#             - name: MovieFilter
