server:
  port: 8080
spring:
  cloud:
    gateway:
      routes:
        - id: catalogRoute
          uri: lb://CATALOG-SERVICE
          predicates:
            - Path=/catalog-api/**
#           filters:
#             - name: CatalogFilter
        - id: movieRoute
          uri: lb://MOVIE-SERVICE
          predicates:
            - Path=/movies-api/**
#           filters:
#             - name: MovieFilter
